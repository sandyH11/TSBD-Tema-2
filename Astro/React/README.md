# React Microservices Frontend - Complete

Estructura con archivos `.jsx` y CSS por componente.

- Ejecutar: `npm install` y `npm start` (puerto 4321).
- Ajustar endpoints en `src/api.js` si es necesario.

Servicios esperados por defecto:
- Students: http://localhost:8080/api/student
- Courses: http://localhost:8081/api/course
